Some pseudo-documentation on how to update this directory.

  Q: If I wanted to update the Extreme packet tests, do I just dump *.p4 into
     npb-master-ptf and *.py into npb-master-ptf/tests?

  A: this is what I did
     - cd npb-master-ptf/src && rm -rf *
     - cd npb-master-ptf/tests && rm -rf *.py
     - Update npb-master-ptf/tests/config.py to add BF_P4C_PATH

       BF_P4C_PATH = os.path.abspath(os.path.dirname(__file__)).replace('/p4c/extensions/p4_tests/p4_16/customer/extreme/npb-master-ptf/tests','')
       TEST_JSON_PATH = os.path.join(BF_P4C_PATH, "p4-tests/p4_16/customer/extreme/npb-master-ptf/tests")
       SDE_INSTALL_PATH = ""

     - Update npb-master-ptf/tests/test.json to add absolute path to output artifacts
